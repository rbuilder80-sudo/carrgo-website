import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CARRGO SEO Master — Live Publishing & Intelligence SaaS",
  description: "Master Agent, live site audits, Google Suggest keyword research, AI content engine and evidence-gated publishing to Medium, LinkedIn, WordPress, Telegram and more for carrgo.co.uk.",
  keywords: ["carrgo.co.uk", "freight forwarding SEO", "publishing automation", "SEO SaaS", "GSC", "GA4"],
  icons: {
    icon: "/downloads/icon128.png",
  },
};


export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
